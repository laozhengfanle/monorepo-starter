# See https://docs.github.com/en/get-started/getting-started-with-git/ignoring-files for more about ignoring files.

# compiled output
dist
tmp
out-tsc

# 本地验证脚本（headless 浏览器等）
.tmp/

# dependencies
node_modules

# TypeDoc 输出（docs.yml 的 typedoc --out docs/api-reference，防误提交）
docs/api-reference/

# Prisma 生成的客户端（由 prisma generate 生成，不提交）
apps/server/src/generated/

# IDEs and editors
/.idea
.project
.classpath
.c9/
*.launch
.settings/
*.sublime-workspace

# IDE - VSCode
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json

# env（本地环境文件不入库，示例文件保留）
.env
!.env.example

# misc
/.sass-cache
/connect.lock
/coverage
/libpeerconnection.log
npm-debug.log
yarn-error.log
testem.log
/typings

# pnpm store（曾配置为项目内 store，防止误提交 57MB 缓存）
/.pnpm-store

# System Files
.DS_Store
Thumbs.db

.nx/cache
.nx/workspace-data
.cursor/rules/nx-rules.mdc
.github/instructions/nx.instructions.md

vite.config.*.timestamp*
vitest.config.*.timestamp*
test-output

.claude/worktrees
.claude/settings.local.json
.nx/polygraph
.nx/self-healing
.nx/migrate-runs
apps/server/uploads/

# 生成物（方案 A：不提交，CI/本地现生成——openapi 由 server:generate-openapi 产出，
# schema.gql 由 server 启动时自动生成，erd.md 由 erd:generate 产出）
openapi/openapi.json
apps/server/graphql/schema.gql
docs/erd.md
# server 启动时按 cwd 生成的 schema（nx serve 的 cwd 是仓库根，产物可能落在根 graphql/）
**/graphql/schema.gql
