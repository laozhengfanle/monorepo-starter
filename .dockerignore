# ===== Docker 构建上下文排除 =====
# 排除项必须与 .gitignore 保持同步，避免把大文件/敏感文件打进镜像上下文

# 依赖与构建产物
node_modules
**/node_modules
dist
**/dist
out-tsc
**/out-tsc
test-output
**/test-output
coverage
**/coverage
.nx

# 本地环境与临时文件
.env
**/.env
!.env.example
.tmp/
uploads/
**/uploads/
.DS_Store
.git
.gitignore
.pnpm-store

# 文档 / 编排 / CI / git hooks（构建不需要，减小上下文体积）
docs/
k8s/
.github/
.husky/

# 编辑器 / AI 配置（不需要进镜像）
.vscode
.idea
.claude
.cursor
.codex
.gemini
.opencode
.agents

# 生成文件（构建期重新生成）
# 注意：只排除 server 的 prisma 生成客户端（不入库）；admin 的 graphql.ts 与
# api-client 的 generated/*.ts 是**入库**的，vite 构建需要它们，不能排除（曾用
# **/generated 误排除导致 admin 镜像构建失败）
apps/server/src/generated
pnpm-debug.log*
*.tsbuildinfo
