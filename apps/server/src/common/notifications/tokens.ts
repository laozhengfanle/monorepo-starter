/** SMS Provider 集合的 DI token（多 Provider 数组注入） */
export const SMS_PROVIDER_TOKEN = Symbol('SMS_PROVIDERS');

/** Email Provider 集合的 DI token（多 Provider 数组注入） */
export const EMAIL_PROVIDER_TOKEN = Symbol('EMAIL_PROVIDERS');
